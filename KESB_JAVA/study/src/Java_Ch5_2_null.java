public class Java_Ch5_2_null {
    public static void main(String[] args) {
        int[] intArray = null;
        //intArray[0] = 10;
        // NullPointerException -> intArray[]가 'null',참조하는 배열 객체가 없으므로 10을 저장할 수 없음

        String str = null;
        //System.out.println("총 문자 수: " + str.length());
        // NullPointerException -> str 변수가 참조하는 String 객체가 없으므로 문자열의 길이를 구할 수 없기 때문이다.

        int[]

    }
}
